# Vercel / local env for CryptoBriefs Dashboard (Vite)
# Backend base URL (no trailing slash)
VITE_API_BASE=https://ces.dbrata.my.id

# Simple admin auth key (send as x-admin-key). Needs to match backend env.
VITE_ADMIN_KEY=caesarganteng
